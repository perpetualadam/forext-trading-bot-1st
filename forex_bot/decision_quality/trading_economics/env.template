# Research-only template. Copy values into a local shell — do NOT put a real
# key in .env, git, Docker, or this file.
#
#   set TRADING_ECONOMICS_API_KEY=   (Windows PowerShell: $env:TRADING_ECONOMICS_API_KEY="...")
#
# The downloader reads this environment variable. It never writes the key to
# raw metadata, logs, or destination files.

TRADING_ECONOMICS_API_KEY=
